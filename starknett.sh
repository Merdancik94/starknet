#!/bin/bash
echo apt-get update && sudo apt install python3-pip -y && sudo apt install build-essential libssl-dev libffi-dev python3-dev -y && sudo apt-get install libgmp-dev -y && pip3 install fastecdsa && sudo apt-get install pkg-config -y && apt install curl -y && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh && source $HOME/.cargo/env && sudo apt install cargo -y && rustup update stable && apt install git -y && git clone --branch v0.1.6-alpha https://github.com/eqlabs/pathfinder.git && sudo apt install python3.8-venv -y && cd pathfinder/py && python3 -m venv .venv && source .venv/bin/activate && PIP_REQUIRE_VIRTUALENV=true pip install --upgrade pip && PIP_REQUIRE_VIRTUALENV=true pip install -r requirements-dev.txt && pytest && cargo build --release --bin pathfinder
